# 301 Redirects - Domain Consolidation
# Note: Domain-level redirects (non-www to www) are configured in Cloudflare Redirect Rules
# _redirects file only handles path-based redirects within the same domain

# Redirect Cloudflare Pages domain to primary domain (handled by Cloudflare)
# https://*.pages.dev/* https://www.spy007.top/:splat 301!

# Client-side routing - must be last
/*  /index.html  200
